name: Tests

on: pull_request

jobs:
  run-tests:
    timeout-minutes: 30
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm

      - name: Print Versions
        run: |
          node --version
          pnpm --version

      - name: Determine Changed Paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'common/**'
            frontend:
              - 'frontend/**'
              - 'common/**'
            common:
              - 'common/**'
      
      - name: Install Common Dependencies
        if: steps.filter.outputs.common == 'true'
        working-directory: common
        run: pnpm install --frozen-lockfile

      - name: Run Common Unit Tests
        if: steps.filter.outputs.common == 'true'
        working-directory: common
        run: pnpm test
        env:
          CI: true

      - name: Install Backend Dependencies
        if: steps.filter.outputs.backend == 'true'
        working-directory: backend
        run: pnpm install --frozen-lockfile

      - name: Run Backend Unit Tests
        if: steps.filter.outputs.backend == 'true'
        working-directory: backend
        run: pnpm test
        env:
          CI: true

      # Copied from https://nextjs.org/docs/pages/building-your-application/deploying/ci-build-caching#github-actions
      - name: NextJS Cache
        if: steps.filter.outputs.frontend == 'true'
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/frontend/.next/cache
          # Generate a new cache whenever frontend packages or source files change.
          key: ${{ runner.os }}-nextjs-${{ hashFiles('frontend/pnpm-lock.yaml', 'common/pnpm-lock.yaml') }}-${{ hashFiles('frontend/**/*.js', 'frontend/**/*.jsx', 'frontend/**/*.ts', 'frontend/**/*.tsx', 'common/**/*.ts', 'common/**/*.tsx') }}
          # If source files changed but frontend packages didn't, rebuild from a prior cache.
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('frontend/pnpm-lock.yaml', 'common/pnpm-lock.yaml') }}-

      - name: Install Frontend Dependencies
        if: steps.filter.outputs.frontend == 'true'
        working-directory: frontend
        run: pnpm install --frozen-lockfile
      
      - name: Run Frontend Unit Tests
        if: steps.filter.outputs.frontend == 'true'
        working-directory: frontend
        run: pnpm test:unit
      
      - name: Install Playwright Browsers
        if: steps.filter.outputs.frontend == 'true'
        working-directory: frontend
        run: npx playwright install --with-deps

      - name: Build Frontend
        if: steps.filter.outputs.frontend == 'true'
        working-directory: frontend
        run: pnpm build:test

      - name: Run Tests
        if: steps.filter.outputs.frontend == 'true'
        working-directory: frontend
        run: pnpm test:playwright
        env:
          CI: true

      - name: Save Playwright Report
        uses: actions/upload-artifact@v5
        if: ${{ !cancelled() }} && steps.filter.outputs.frontend == 'true'
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 14
      
      # - name: Check Coverage
      #   working-directory: frontend
      #   run: npx nyc report --reporter=text-summary >> $GITHUB_STEP_SUMMARY

      # - name: Upload Coverage Report
      #   uses: actions/upload-artifact@v4
      #   if: always()
      #   with:
      #     name: coverage
      #     path: frontend/coverage
